package Utilities;

public class Constants {
    //this is the information that we will rarely be changing so, we can declare a constant variable CONFIG_FILE_PATH
    public static final String CONFIG_FILE_PATH=System.getProperty("user.dir") + "\\Files\\Config.properties";
    public static final String EXCEL_FILE_PATH=System.getProperty("user.dir") + "\\Files\\PropertiesHW1.xlsx";
}
